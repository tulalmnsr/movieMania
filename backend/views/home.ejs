<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movies.length > 0 ? movies[0].title : 'No Movies' %></title>
</head>
<body>
    <h1>Movies</h1>
    <% if (movies.length > 0) { %>
        <ul>
            <% movies.forEach(movie => { %>
                <li><%= movie.title %></li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>No movies available.</p>
    <% } %>
</body>
</html>